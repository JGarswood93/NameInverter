package com.barton;

import java.util.List;

public class NameInvers {

    public static String invert(String Name) {//data type and variable name
        List<String>nameParts = List.newArrayList();
        String first = Name.trim();
        String last;
        return String.format("%s", "%s", last, first);
    }
}
